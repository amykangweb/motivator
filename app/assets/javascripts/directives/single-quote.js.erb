(function(){

var app = angular.module('quotes');

app.directive('singleQuote', function(){
  return {
    templateUrl: '<%= asset_path("single-quote.html.erb") %>',
    controller: 'quotesCtrl',
    replace: true
  }
});

})();
